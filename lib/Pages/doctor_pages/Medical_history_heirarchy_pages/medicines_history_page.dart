import 'package:flutter/material.dart';

class MedicinesHistoryPage extends StatelessWidget {
  static final String id = "MedicinesHistoryPage";
  const MedicinesHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Medicines :"),
      ),
    );
  }
}
