import 'package:flutter/material.dart';

class RequiredImagingPage extends StatelessWidget {
  static const String id = 'RequiredImagingPage';
  const RequiredImagingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Required Imaging : '),
      ),
    );
  }
}
